A. create a folder name "ffmpeg" inside C://Program Files//
B. extract the ffmpeg-2020-09-20-git-ef29e5bf42-full_build.rar file inside "ffmpeg" folder you created in step A.
C. now go to environment variable and add the mentioned path of ffmpeg ---> C://Program Files//ffmpeg//bin
do these steps for either methods, since it is required for both.

Recommended Method :
1. open your command prompt or powershell window with the same path as this folder.
2. type ```.\iras\Script\activate```, press enter.
	-- if you are having error in activate virtual environment then open command prompt or powershell as administrator.
	-- now type ```set-executionpolicy remotesigned```, press enter, now repeat step 2.
3. after activating virtual environment, the path should look like this ```(iras) .\your-path\Indian_Railway_Announcement_System```.
4. now run the program using this command ```python iras.py```.
5. If you would like to see the data of trains which were used in this program then open railway.xlsx file.


Alternate Method :
In case you don't want to use virtual environment which was created already then follow these steps:
1. open your command prompt or powershell window with the same path as this folder.
2. execute these commands.
3. ```pip install -r requirements.txt``` in either powershell or command prompt as administration.
4. now install pyaudio using these commands.
	-- ```pip install pipwin```.
	-- ```pipwin install pyaudio```.
5. run the program using this command ```python iras.py```.